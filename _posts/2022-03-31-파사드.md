---
title: "Facade"
excerpt: "데코레이터의 기본 개념 및 예제"

categories:
  - Design Pattern
tags:
  - facade
  - java

toc: true
toc_sticky: true
---

## Facade pattern

Facade는 건물의 앞쪽 정면이라는 뜻으로, 여러 개의 객체와 실제 사용하는 서브 객체의 사이에 복잡한 의존 관계가 있을 때, 중간에 facade 라는 객체를 두고, 여기서 제공하는 인터페이스만을 활용해 기능을 사용하는 방식

Facade는 자신이 가지고 있는 각 클래스의 기능을 명확히 알아야한다.

## Facade 예제

Ftp, Reader, Writer라는 객체를 각각 만들어서 메인에서 실행시키려면 아래와 같다.

```java
public static void main(String[] args) {
		// Ftp 객체를 불러와 실행
    Ftp ftpClient = new Ftp("www.foo.co.kr", 22, "/home/etc");
    ftpClient.connect();
    ftpClient.moveDirectory();

		// Writer 객체를 불러와 실행
    Writer writer = new Writer("text.tmp");
    writer.fileConnect();
    writer.write();

		// Reader 객체를 불러와 실행
    Reader reader = new Reader("text.tmp");
    reader.fileConnect();
    reader.fileRead();

		// disconnect 하기
    reader.fileDisconnect();
    writer.fileDisconnect();
    ftpClient.disConnect();
}
```

하지만 이렇게 각각의 객체들을 묶어서 Facade 패턴처럼 만들면 사용하기 더 좋을 것이다.

### Facade 클래스

```java
package com.company.facade;

public class SftpClient {
    private Ftp ftp;
    private Writer writer;
    private Reader reader;

		// 세개의 객체를 받아오는 생성자
    public SftpClient(Ftp ftp, Reader reader, Writer writer){
        this.ftp = ftp;
        this.writer = writer;
        this.reader = reader;
    }
		// 생성자를 오버로딩
    public SftpClient(String host, int port, String path, String fileName){
        this.ftp = new Ftp(host, port, path);
        this.reader = new Reader(fileName);
        this.writer = new Writer(fileName);
    }

		// 새로운 메서드들
    public void connect(){
        ftp.connect();
        ftp.moveDirectory();
        writer.fileConnect();
        reader.fileConnect();
    }

    public void disconnect(){
        writer.fileDisconnect();
        reader.fileDisconnect();
        ftp.disConnect();
    }

    public void read(){
        reader.fileRead();
    }
    public void write(){
        writer.write();
    }

}

```

위와같이 파사드 패턴을 사용하면 아래와 같이 하나의 클래스만 들고와 깔끔하게 만들 수 있다.

```java
public static void main(String[] args) {
		SftpClient sftpClient = new SftpClient("www.foo.co.kr", 22, "/home/etc", "text.tmp");
        sftpClient.connect();
        sftpClient.write();
        sftpClient.read();
        sftpClient.disconnect();
}
```