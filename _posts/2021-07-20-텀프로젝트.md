---
title: "Baekjoon 9466번 텀 프로젝트 [Python]"
excerpt: "dfs, cycle 판단"

categories:
  - Baekjoon
tags:
  - dfs
  - cycle
  - find & union
  - gold4
toc: true
toc_sticky: true
---


### 문제

![](https://jihun-24k.github.io/assets/images/baek_img/term1.png){: .align-center}

- 각각 학생은 조별과제를 위해 파트너를 한명만 정해야 한다.
- 자기 자신을 선택할 수도 있다.
- 조가 없는 사람들의 수를 리턴
    
---

### 문제 접근 방법

- 그래프 중에서 사이클 판별을 해야한다.
- dfs로 각 노드의 부모 노드를 찾는다.
- 슬라이싱으로 사이클의 길이를 구해준다.

---

### 풀이 코드

```python
# 백준 9466번 텀 프로젝트
import sys
sys.setrecursionlimit(10**9)
input = sys.stdin.readline

T = int(input())
ans = []

def find_team(x, result):
    visited[x] = True
    cycle.append(x)
    num = graph[x]

    # 종료 조건
    if visited[num]:
        if num in cycle:
            result += cycle[cycle.index(num):]
        return
    else:
        find_team(num, result)

# 테스트 케이스마다 반복
for _ in range(T):
    V = int(input())
    visited = [False for _ in range(V + 1)]
    graph = [0]+list(map(int,input().split()))
    result = []

    # 케이스들의 노드들을 확인
    for idx in range(1,V + 1):
        if not visited[idx]:
            cycle = []
            find_team(idx, result)
    ans.append(V- len(result))

for a in ans:
    print(a)
```
---

### 문제 후기

처음 접근해서 풀땐 재귀 조건들이 너무 많았는지 런타임 에러가 떴다. 이후에 수정한 뒤 메모리 초과를 해결하지 못했었는데 테스트 케이스를 따로 for문 안에 append로 받아서 뜬 것이였다. 되도록 입력은 <b>comphrehesion</b> 방법으로 받아야 하는거 같다.

![](https://jihun-24k.github.io/assets/images/baek_img/term2.png){: .align-center}


---